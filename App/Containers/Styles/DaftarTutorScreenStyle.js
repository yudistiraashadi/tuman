import { StyleSheet, Dimensions } from "react-native";

export default StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#FFF"
  },
  input: {
    textAlign: "center",
    color: "#000"
  },
  inputWrapper: {
    marginBottom: 10
  },
  inputError: {
    color: "red",
    fontSize: 14,
    marginTop: -10,
    marginBottom: 10
  }
});
